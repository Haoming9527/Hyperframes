# Kit contents

| Item | Path |
|------|------|
| Skills | `.skills/hyperframes/skills/` |
| CLI | `node_modules/` |
| FFmpeg | `vendor/ffmpeg/bin/` |
| Devin rules | `AGENTS.md` |
| Env template | `.env.example` (checklist for CMD `set` — not auto-loaded) |

On the PC: Node.js + npm + Chrome. Usage: see `README.md` (`npx hyperframes …`).
